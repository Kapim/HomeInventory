<UserControl x:Class="HomeInventory.Desktop.Wpf.Views.TopBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HomeInventory.Desktop.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Household switch -->
        <ComboBox Grid.Column="0"
                  ItemsSource="{Binding Households}"
                  SelectedItem="{Binding SelectedHousehold}"
                  DisplayMemberPath="Name"
                  MinHeight="32"/>

        <!-- Search -->
        <TextBox Grid.Column="2"
                 Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                 MinHeight="32"
                 VerticalContentAlignment="Center"
                 ToolTip="Hledat (Ctrl+K)"/>

        <!-- Actions -->
        <StackPanel Grid.Column="4" Orientation="Horizontal">
            <Button Content="+ Item" Command="{Binding AddItemCommand}" Margin="0,0,8,0"/>
            <Button Content="+ Location" Command="{Binding AddLocationCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
