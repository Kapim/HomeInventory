<UserControl x:Class="HomeInventory.Desktop.Wpf.Views.ItemsSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HomeInventory.Desktop.Wpf.Views"
             xmlns:vm="clr-namespace:HomeInventory.Desktop.Wpf.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{StaticResource AppBackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Vyhledávání" Style="{StaticResource PageTitleText}" />
                <TextBlock Text="Najděte položky a lokace napříč domácností." Style="{StaticResource CaptionText}" Margin="0,4,0,0" />
            </StackPanel>
            <Button Grid.Column="1" Content="← Zpět" Command="{Binding GoBackCommand}" MinHeight="38" Padding="14,0" Margin="0,0,8,0" />
            <Button Grid.Column="2" Content="Odhlásit" Command="{Binding LogoutCommand}" MinHeight="38" Padding="14,0" />
        </Grid>

        <!-- Household selector -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Domácnost:" VerticalAlignment="Center" Margin="0,0,10,0" Style="{StaticResource CaptionText}" />
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Households}"
                      SelectedItem="{Binding SelectedHousehold}"
                      DisplayMemberPath="Name" />
        </Grid>

        <!-- Search bar -->
        <Grid Grid.Row="2" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     MinHeight="42"
                     Margin="0,0,10,0"
                     materialDesign:HintAssist.Hint="Hledaný výraz (hledá průběžně při psaní)"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="Hledat" Command="{Binding SearchCommand}" Style="{StaticResource PrimaryActionButton}" />
        </Grid>

        <!-- Results -->
        <Border Grid.Row="3" Style="{StaticResource SoftPanelBorder}" Padding="8">
            <ListBox ItemsSource="{Binding Results}"
                     SelectedItem="{Binding SelectedResult}"
                     BorderThickness="0"
                     Background="Transparent"
                     Cursor="Hand">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:SearchResult}">
                        <Grid Margin="4,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0"
                                    Background="{StaticResource AppAccentBrush}"
                                    CornerRadius="4"
                                    Padding="6,2"
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center">
                                <TextBlock Text="{Binding Type}" Foreground="White" FontSize="11" />
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding LocationPath}" Style="{StaticResource CaptionText}" Margin="0,2,0,0" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
